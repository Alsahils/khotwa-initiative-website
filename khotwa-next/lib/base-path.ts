/**
 * The base path for all asset URLs when deploying to GitHub Pages.
 * Set via NEXT_PUBLIC_BASE_PATH env variable (inlined at build time by Next.js).
 */
export const BASE_PATH = process.env.NEXT_PUBLIC_BASE_PATH || "";
